server:
  port: 9001
  contextPath: /

security:
  oauth2:
    resource:
      user-info-uri: http://localhost:9000/auth/user

logging:
  level:
    org.hibernate.SQL: debug

spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
